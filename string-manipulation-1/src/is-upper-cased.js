/* exported isUpperCased */
// if word is capatalized return true
// else return false
function isUpperCased(word) {
  if (word === word.toUpperCase(word)) {
    return true;
  } else {
    return false;
  }
}
