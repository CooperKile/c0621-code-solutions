/* exported isLowerCased */
// check if word is lowercased by setting it strictly equal to toLowerCase
// if true return true
// if else return false
function isLowerCased(word) {
  if (word === word.toLowerCase(word)) {
    return true;
  } else {
    return false;
  }
}
